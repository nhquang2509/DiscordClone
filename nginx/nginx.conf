events {
    worker_connections 1024;
}

http {

    upstream nextjs_cluster {
        server web:3000;
    }

    server {
        listen 80;
        server_name discord.nhquang.id.vn;

        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name discord.nhquang.id.vn;

        ssl_certificate /etc/letsencrypt/live/discord.nhquang.id.vn/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/discord.nhquang.id.vn/privkey.pem;

        location / {
            proxy_pass http://nextjs_cluster;

            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            proxy_set_header Host $host;

            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;

            # Increase proxy buffers for large Next.js headers
            proxy_buffer_size          128k;
            proxy_buffers              4 256k;
            proxy_busy_buffers_size    256k;
        }
    }
}